---
title: Presence API
description: Usa la API de Presence de Nowly para actualizar Discord Rich Presence desde una integración web.
---

## Creando una Presence

`Presence` es un **global** inyectado por el runtime de Nowly — no necesitas importarlo.

Toda presence comienza con una instancia de `Presence`:

```ts filename="presence.ts"
const presence = new Presence()
```

## Evento UpdateData

Usa `UpdateData` para actualizar la actividad:

```ts filename="presence.ts"
presence.on("UpdateData", async () => {
  await presence.setActivity({
    details: "Viendo un video",
  })
})
```

## setActivity

`setActivity` envía la actividad actual de Discord. Los campos comunes incluyen:

<TypeTable
  properties={[
    { name: "details", type: "string", description: "Línea de actividad principal mostrada en Discord." },
    { name: "state", type: "string", description: "Línea de actividad secundaria mostrada debajo de detalles." },
    { name: "startTimestamp", type: "number", description: "Marca de tiempo Unix para el progreso de actividad transcurrido." },
    { name: "endTimestamp", type: "number", description: "Marca de tiempo Unix para el progreso de actividad restante." },
    { name: "largeImageKey", type: "string", description: "Clave de imagen principal, generalmente Assets.Logo o Assets.Thumbnail (globales proporcionados por el runtime)." },
    { name: "largeImageText", type: "string", description: "Tooltip para la imagen grande." },
    { name: "smallImageKey", type: "string", description: "Clave de imagen pequeña opcional." },
    { name: "smallImageText", type: "string", description: "Tooltip para la imagen pequeña." },
    { name: "type", type: "number", description: "Tipo de actividad de Discord de PresenceType." },
    { name: "buttons", type: "array", description: "Botones de actividad de Discord opcionales." }
  ]}
/>

## clearActivity

Llama a `clearActivity` cuando el usuario ya no esté haciendo algo que valga la pena mostrar:

```ts filename="presence.ts"
presence.clearActivity()
```

## PresenceType

`PresenceType` es una exportación nombrada de `@nowly/sdk`:

```ts filename="presence.ts"
import { PresenceType } from "@nowly/sdk"
```

Se asigna a los tipos de actividad de Discord: `Playing`, `Streaming`, `Listening`, `Watching` y `Competing`.

## Registro

Usa `presence.info` y `presence.error` para mensajes de depuración durante el desarrollo.
