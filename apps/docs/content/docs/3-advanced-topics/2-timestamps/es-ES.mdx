---
title: Marcas de tiempo
description: Muestra el progreso de reproducción en Discord Rich Presence con marcas de tiempo multimedia.
---

## Por qué importan las marcas de tiempo

Las marcas de tiempo hacen que la actividad de Discord se sienta en vivo. Muestran cuánto tiempo lleva viendo el usuario o cuánto tiempo queda.

## Usar createMediaTimestamps

`document` es la API DOM del navegador (disponible porque las presencias se ejecutan como scripts de contenido). `Assets` lo proporciona el runtime (consulta [Assets](/presence-development/assets)).

```ts filename="presence.ts"
import { createMediaTimestamps } from "@nowly/sdk"

const video = document.querySelector("video")

if (video) {
  await presence.setActivity({
    details: document.title,
    largeImageKey: Assets.Logo,
    ...createMediaTimestamps(video),
  })
}
```

## Multimedia en pausa

Cuando el multimedia está en pausa, `createMediaTimestamps` no devuelve marcas de tiempo. Así se evita mostrar un progreso de reproducción incorrecto.

## Transmisiones en vivo

Las transmisiones en vivo a menudo no tienen una duración fija. Usa solo una marca de tiempo de inicio u omite las marcas de tiempo según el comportamiento de la plataforma.
