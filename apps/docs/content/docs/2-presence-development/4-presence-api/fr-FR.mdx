---
title: Presence API
description: Utilisez l'API Presence de Nowly pour mettre à jour Discord Rich Presence depuis une intégration web.
---

## Créer une présence

`Presence` est une **globale** injectée par le runtime Nowly — vous n'avez pas besoin de l'importer.

Chaque présence commence par une instance de `Presence`&nbsp;:

```ts filename="presence.ts"
const presence = new Presence()
```

## Événement UpdateData

Utilisez `UpdateData` pour actualiser l'activité&nbsp;:

```ts filename="presence.ts"
presence.on("UpdateData", async () => {
  await presence.setActivity({
    details: "Regarde une vidéo",
  })
})
```

## setActivity

`setActivity` envoie l'activité Discord actuelle. Les champs courants incluent&nbsp;:

<TypeTable
  properties={[
    { name: "details", type: "string", description: "Ligne d'activité principale affichée dans Discord." },
    { name: "state", type: "string", description: "Ligne d'activité secondaire affichée sous les détails." },
    { name: "startTimestamp", type: "number", description: "Horodatage Unix pour la progression de l'activité écoulée." },
    { name: "endTimestamp", type: "number", description: "Horodatage Unix pour la progression de l'activité restante." },
    { name: "largeImageKey", type: "string", description: "Clé d'image principale, généralement Assets.Logo ou Assets.Thumbnail (globales fournies par le runtime)." },
    { name: "largeImageText", type: "string", description: "Infobulle de la grande image." },
    { name: "smallImageKey", type: "string", description: "Clé d'image petite facultative." },
    { name: "smallImageText", type: "string", description: "Infobulle de la petite image." },
    { name: "type", type: "number", description: "Type d'activité Discord depuis PresenceType." },
    { name: "buttons", type: "array", description: "Boutons d'activité Discord facultatifs." }
  ]}
/>

## clearActivity

Appelez `clearActivity` lorsqu'il n'y a plus rien à afficher&nbsp;:

```ts filename="presence.ts"
presence.clearActivity()
```

## PresenceType

`PresenceType` est un export nommé de `@nowly/sdk`&nbsp;:

```ts filename="presence.ts"
import { PresenceType } from "@nowly/sdk"
```

Il correspond aux types d'activité Discord&nbsp;: `Playing`, `Streaming`, `Listening`, `Watching` et `Competing`.

## getSetting

Lisez un paramètre typé après `Presence.Settings`. La valeur est le réglage enregistré de l'utilisateur, ou la valeur par défaut de la définition&nbsp;:

```ts filename="presence.ts"
const showButtons = await presence.getSetting<boolean>("showButtons")
```

`UpdateData` reçoit aussi `ctx.settings` avec la même correspondance. Les deux se mettent à jour lorsque l'utilisateur change les réglages dans le panneau.

## Journalisation

Utilisez `presence.info` et `presence.error` pour les messages de débogage pendant le développement.
