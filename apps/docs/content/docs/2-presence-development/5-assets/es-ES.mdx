---
title: Recursos
description: Prepara el logo, el icono y la miniatura para las presencias de Nowly.
---

## Recursos requeridos

Cada presencia debe incluir:

<TypeTable
  properties={[
    { name: "logo.png", type: "PNG, 300×300", description: "Marca de la plataforma para colocaciones amplias, encabezados e imágenes Open Graph.", required: true },
    { name: "icon.png", type: "PNG, 128×128", description: "Icono cuadrado compacto usado en superficies de interfaz pequeñas e imágenes de actividad de Discord.", required: true },
    { name: "thumbnail.jpg", type: "JPG, 1920×1080", description: "Visual de la biblioteca y página de detalle que represente claramente la plataforma.", required: true }
  ]}
/>

Estas dimensiones exactas se verifican en CI en cada PR de presencia - un recurso con el tamaño incorrecto hace fallar la validación.

## Logo

Usa `logo.png` para la marca de la plataforma cuando convenga una imagen más amplia. Debe medir exactamente **300×300**.

## Icono

Usa `icon.png` para pantallas cuadradas compactas, fondos de color e imágenes de actividad de Discord. Debe medir exactamente **128×128**.

## Miniatura

Usa `thumbnail.jpg` para los visuales de la biblioteca y las páginas de detalle. Debe representar la plataforma con claridad. Debe medir exactamente **1920×1080**.

## Constantes de recursos en runtime

`Assets` lo proporciona el runtime (consulta [Presence API](/presence-development/presence-api)). Expone las tres claves de recursos predeterminadas:

```ts filename="presence.ts"
Assets.Logo      // → "logo.png"
Assets.Icon      // → "icon.png"
Assets.Thumbnail // → "thumbnail.jpg"
```

Úsalas en los datos de actividad:

```ts filename="presence.ts"
await presence.setActivity({
  largeImageKey: Assets.Logo,
  largeImageText: "YouTube",
})
```

## Directrices de calidad

Usa un PNG transparente para `icon.png` - el icono se muestra sobre un fondo de color en la biblioteca de Nowly, así que un fondo relleno chocará. Se prefiere un `logo.png` transparente, aunque es menos crítico. Mantén las miniaturas legibles en tamaños pequeños y evita imágenes genéricas.
