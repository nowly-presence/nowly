node_modules
dist
.next
*.tsbuildinfo

# Native host (Go binaries)
apps/native/dist/

# Extension (managed separately)
# apps/extension/
apps/discord/
apps/mvp-chromium-extension/
old-native/

# Local environment variables
.env
.env.local

# Extension signing key (keep secret!)
extension-key.pem

# macOS signing material (keep secret! never commit - use GitHub Secrets)
*.p8
*.p12
*.cer
*.certSigningRequest

# Auto-generated extension source
apps/extension/src/generated/*
apps/extension/artifacts/
